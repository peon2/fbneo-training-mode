Training Mode:
  Todo:
    ☐ fix detection of guard in random so it lasts for the duration of the move
    config:
      ☐ create the other config access
      ☐ add stun stats to the screen for both players
      ☐ add last stun taken to the screen for both players
      ☐ add guard points for both players
      ☐ add last guard damage taken
      ☐ add screen UI for other configurations
      ☐ add enable|disable stun for both players
      ☐ add enable|disable guard break for both players
      

